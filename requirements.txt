# Core framework
fastapi==0.111.0
uvicorn[standard]==0.30.0

# Database & ORM
SQLAlchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9  # For PostgreSQL sync driver
# asyncpg==0.29.0        # Uncomment if using async SQLAlchemy instead

# Data validation
pydantic==2.8.2
pydantic-settings==2.3.0

# Authentication & security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.2.0  # for user email validation

# Redis & caching
redis==5.0.5
aioredis==2.0.1         # Only if using async (WS presence, queue)

# Background tasks
celery==5.3.6
# or rq==1.16.1          # If you prefer RQ, uncomment and remove celery

# WebSocket & realtime
websockets==12.0

# Rate limiting (optional, for production)
slowapi==0.1.8

# Testing & linting
pytest==8.2.2
httpx==0.27.0           # for async test client
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Utilities
python-dotenv==1.0.1
loguru==0.7.2           # for better logging
